import React, {useState} from 'react'

const Content = () => {
  const [items, setItems] = useState([
    {id: 1, desc: 'um'},
    {id: 2, desc: 'dois'},
    {id: 3, desc: 'três'}
  ])
  return
}

export default Content